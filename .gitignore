# Foundry artifacts (only relevant under assets/fixtures/)
assets/fixtures/out/
assets/fixtures/cache/
assets/fixtures/broadcast/
assets/fixtures/lib/

# OS junk
.DS_Store
Thumbs.db

# Editor temp
*.swp
*.swo
*~
.idea/
.vscode/

# Holdings cache produced by eligibility runs (per-user, never committed)
holdings/

# Test runner output (logs, extracted bash blocks, scratch fixtures)
tests/_out/

# `skills add` artifacts when run from inside this directory
.agents/
skills-lock.json

# Local environment
.env
.env.local
